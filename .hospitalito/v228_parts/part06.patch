+      const u=users.find(x=>String(x.username||"")===selectedMember)||findUserByUsername?.(selectedMember);
+      if(!u){selectedMember="";return renderMembers();}
+      if(memberChat===selectedMember)return renderMemberChat(u);
+      const acts=(getActivity?.()||[]).filter(a=>String(a.user||"")===selectedMember).slice().reverse().slice(0,15);
+      return `<div class="hcSettingsPage">${hero("Miembros","Perfil ampliado dentro de la nueva Configuración.")}<div class="hcMemberProfileSurface" style="${memberThemeStyle(u)}">
+        <div class="hcMemberProfileHead">${avatarHtml(u)}<div class="hcSettingsMeta"><div class="hcSettingsName">${esc(u.name||u.username)}</div><div class="hcSettingsMuted">@${esc(u.username)} · ${esc(roleLabel(u.role))}</div><div class="hcSettingsMuted" style="margin-top:5px;">Tema personal: ${esc(THEME_DEFS[userTheme(u)]?.label||"Azul oscuro")}</div><div class="hcMemberProfileActions"><button class="hcSettingsBtn" id="hcsMembersBack">← Miembros</button><button class="hcSettingsBtn primary" data-hcs-chat="${esc(u.username)}">Mensajes</button></div></div></div>
+        <div class="hcMemberProfileBody"><div class="hcSettingsGrid"><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Perfil profesional</div><div class="hcToggleRow"><div><div class="hcToggleTitle">Especialidad / función</div><div class="hcToggleSub">${esc(u.specialty||"—")}</div></div></div><div class="hcToggleRow"><div><div class="hcToggleTitle">Matrícula</div><div class="hcToggleSub">${esc(u.licenseNumber||"—")}</div></div></div></div><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Actividad reciente</div><div class="hcMemberActivity">${acts.length?acts.map(a=>`<div class="hcMemberActivityItem"><div class="hcMemberActivityTime">${esc(typeof dmyhmFromISODateTime==="function"?dmyhmFromISODateTime(a.ts):a.ts||"")}</div><div class="hcMemberActivityText">${esc(a.message||"—")}</div></div>`).join(""):`<div class="hcSettingsMuted">Sin actividad registrada.</div>`}</div></div></div></div>
+      </div></div>`;
+    }
+    return `<div class="hcSettingsPage">${hero("Miembros","Perfiles de la institución, actividad y mensajes en una vista más amplia.")}<div class="hcSettingsCard"><div class="hcMemberToolbar"><input id="hcsMemberSearch" placeholder="Buscar por nombre, usuario o rol"><span class="hcSettingsMuted">${users.length} miembros</span></div><div class="hcMemberListNew" id="hcsMemberList">${users.length?users.map(u=>`<button class="hcMemberCardNew" type="button" data-hcs-member="${esc(u.username)}" data-search="${esc((u.name+" "+u.username+" "+u.role).toLowerCase())}"><span class="hcMemberMiniAvatar">${u.photo?`<img src="${u.photo}" alt="">`:`<span class="hcSettingsMuted">${esc((u.name||u.username||"?").slice(0,1).toUpperCase())}</span>`}</span><span style="min-width:0;text-align:left;"><span style="display:block;font-weight:850;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${esc(u.name||u.username)}</span><span class="hcSettingsMuted" style="display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">@${esc(u.username)} · ${esc(roleLabel(u.role))}</span></span></button>`).join(""):`<div class="hcSettingsMuted">No hay otros miembros disponibles.</div>`}</div></div></div>`;
+  }
+  function renderMemberChat(u){
+    const chats=getChats?.()||{};const k=typeof chatKey==="function"?chatKey(meName(),u.username):[meName(),u.username].sort().join("__");const msgs=Array.isArray(chats[k])?chats[k]:[];
+    return `<div class="hcSettingsPage">${hero("Mensajes",`Conversación con ${u.name||u.username}`)}<div class="hcMemberProfileSurface" style="${memberThemeStyle(u)}"><div class="hcMemberProfileHead">${avatarHtml(u)}<div class="hcSettingsMeta"><div class="hcSettingsName">${esc(u.name||u.username)}</div><div class="hcSettingsMuted">@${esc(u.username)} · ${esc(THEME_DEFS[userTheme(u)]?.label||"Azul oscuro")}</div><div class="hcMemberProfileActions"><button class="hcSettingsBtn" id="hcsChatBack">← Perfil</button></div></div></div><div class="hcMemberProfileBody"><div class="hcSettingsChat" style="${memberThemeStyle(u)}"><div class="hcSettingsChatMsgs" id="hcsChatMsgs">${msgs.map(m=>{const mine=String(m.from||"")===meName();const stamp=typeof dmyhmFromISODateTime==="function"?dmyhmFromISODateTime(m.ts):m.ts||"";return `<div class="hcSettingsBubble ${mine?"me":""}">${m.text?`<div>${esc(m.text)}</div>`:""}${m.kind==="image"&&m.dataURL?`<img src="${m.dataURL}" alt="imagen">`:""}<div class="hcSettingsBubbleMeta">${esc(stamp)} · ${mine?"vos":esc(u.name||u.username)}</div></div>`;}).join("")}</div><div class="hcSettingsChatComposer"><input id="hcsChatText" type="text" placeholder="Escribí un mensaje..."><input id="hcsChatImg" type="file" accept="image/*"><button class="hcSettingsBtn primary" id="hcsChatSend" type="button">Enviar</button></div></div></div></div></div>`;
+  }
+  function renderInstitutions(){
+    const rows=typeof hcGlobalStoredInstitutions==="function"?hcGlobalStoredInstitutions():[];const current=typeof hcGlobalCurrentInstitutionId==="function"?hcGlobalCurrentInstitutionId():String(session?.institutionId||"");
+    return `<div class="hcSettingsPage">${hero("Mis instituciones","Ámbitos de trabajo habilitados para tu cuenta.")}<div class="hcSettingsCard"><div class="hcSettingsCardTitle">Instituciones disponibles</div><div class="hcSettingsCardSub">Cambiar de institución mantiene la lógica segura ya existente y recarga su censo correspondiente.</div>${rows.length?rows.map(i=>`<div class="hcInstitutionCardNew ${String(i.id)===String(current)?"hcInstitutionCurrent":""}"><div style="flex:1;min-width:0;"><div style="font-weight:850;">${esc(i.name||"Institución")}</div><div class="hcSettingsMuted">${esc(i.role||"")} ${i.description||i.address?`· ${esc(i.description||i.address)}`:""}</div></div>${String(i.id)===String(current)?`<span class="hcSettingsMuted">Actual</span>`:`<button class="hcSettingsBtn" data-hcs-switch-inst="${esc(i.id)}">Abrir</button>`}</div>`).join(""):`<div class="hcSettingsNotice">No hay una lista global disponible en este equipo. Institución actual: <b>${esc(currentInstitutionName())}</b>.</div>`}</div></div>`;
+  }
+  function renderSignature(){
+    const u=currentUser();

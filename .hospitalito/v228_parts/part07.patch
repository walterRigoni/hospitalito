+    return `<div class="hcSettingsPage">${hero("Firma","Firma gráfica y datos profesionales usados en documentos impresos.")}<div class="hcSettingsGrid"><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Vista previa</div><div class="userSignaturePreview" style="min-height:150px;display:grid;place-items:center;background:#fff;border-radius:10px;border:1px solid #d0d7de;overflow:hidden;">${u.signature?`<img src="${u.signature}" alt="Firma" style="max-width:100%;max-height:140px;object-fit:contain;">`:`<span style="color:#475569;font-size:12px;">Sin firma cargada</span>`}</div><div class="hcSettingsField" style="margin-top:10px;"><label>Adjuntar nueva firma</label><input id="hcsSignatureFile" type="file" accept="image/png,image/jpeg,image/webp"></div></div><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Datos profesionales</div><div class="hcSettingsFields"><div class="hcSettingsField"><label>Título</label><input id="hcsSigTitle" value="${esc(u.professionalTitle||"")}"></div><div class="hcSettingsField"><label>Matrícula</label><input id="hcsSigLicense" value="${esc(u.licenseNumber||"")}"></div><div class="hcSettingsField full"><label>Especialidad / función</label><input id="hcsSigSpecialty" value="${esc(u.specialty||"")}"></div></div><div class="hcSettingsActions"><button class="hcSettingsBtn primary" id="hcsSaveSignature" type="button">Guardar firma y datos</button><button class="hcSettingsBtn" id="hcsRemoveSignature" type="button">Quitar firma</button></div><div id="hcsSignatureMsg" class="hcSettingsNotice">La firma gráfica del prototipo no reemplaza una firma digital criptográfica certificada.</div></div></div></div>`;
+  }
+  function renderPersonalization(){
+    const active=userTheme(currentUser());
+    return `<div class="hcSettingsPage">${hero("Personalización","Elegí la ambientación visual. Los colores clínicos de seguridad permanecen independientes del tema.")}<div class="hcSettingsCard"><div class="hcThemeGrid">${Object.entries(THEME_DEFS).map(([k,d])=>`<button class="hcThemeTile ${k===active?"active":""}" type="button" data-hcs-theme="${k}"><div class="hcThemePreview" style="--p-bg:${d.bg};--p-surface:${d.surface};--p-line:${d.line};--p-accent:${d.accent};"><div class="hcThemePreviewSide"></div><div class="hcThemePreviewMain"><div class="hcThemePreviewBar"></div><div class="hcThemePreviewCard"></div></div></div><div class="hcThemeLabel">${esc(d.label)}</div><div class="hcThemeHint">${esc(d.hint)}</div></button>`).join("")}</div><div class="hcSettingsNotice">Tema actual: <b>${esc(THEME_DEFS[active]?.label||"Azul oscuro")}</b>. Se guarda por usuario en este dispositivo y se intenta asociar al perfil global cuando el backend lo admite.</div></div></div>`;
+  }
+  function switchRow(id,title,sub,checked,disabled=false){return `<div class="hcToggleRow"><div class="hcToggleText"><div class="hcToggleTitle">${esc(title)}</div><div class="hcToggleSub">${esc(sub)}</div></div><label class="hcSwitch"><input id="${id}" type="checkbox" ${checked?"checked":""} ${disabled?"disabled":""}><span class="hcSwitchTrack"></span></label></div>`;}
+  function renderNotifications(){const p=loadPrefs();return `<div class="hcSettingsPage">${hero("Notificaciones","Preferencias de información visual del sistema.")}<div class="hcSettingsGrid"><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Interfaz</div>${switchRow("hcsShowSync","Mostrar estado de sincronización","Muestra el estado Supabase en la barra superior.",p.showSyncStatus!==false)}</div><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Alertas clínicas</div>${switchRow("hcsClinicalAlerts","Alertas clínicas activas","Las alarmas de Enfermería, prescripciones y estados críticos no se desactivan desde Personalización.",true,true)}<div class="hcSettingsNotice warn">Se mantienen fuera del sistema de temas para no perder significado clínico.</div></div></div></div>`;}
+  function renderAccessibility(){const p=loadPrefs();return `<div class="hcSettingsPage">${hero("Apariencia y accesibilidad","Ajustes visuales que no cambian datos clínicos.")}<div class="hcSettingsCard">${switchRow("hcsHighContrast","Contraste reforzado","Engrosa bordes y mejora la lectura de textos secundarios.",!!p.highContrast)}${switchRow("hcsLargeText","Texto ligeramente mayor","Aumenta el tamaño de controles y texto común.",!!p.largeText)}${switchRow("hcsReduceMotion","Reducir animaciones decorativas","Reduce transiciones dentro de Configuración; no apaga alarmas clínicas.",!!p.reduceMotion)}</div></div>`;}
+  function renderSessions(){
+    const mobile=document.body.classList.contains("hcMobile")||matchMedia?.("(pointer:coarse)")?.matches;const orientation=innerWidth>innerHeight?"Horizontal":"Vertical";
+    return `<div class="hcSettingsPage">${hero("Sesiones y dispositivos","Información del dispositivo que está usando esta sesión.")}<div class="hcSettingsGrid"><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Este dispositivo</div><div class="hcToggleRow"><div><div class="hcToggleTitle">Tipo detectado</div><div class="hcToggleSub">${mobile?"Celular / táctil":"PC / escritorio"}</div></div></div><div class="hcToggleRow"><div><div class="hcToggleTitle">Orientación</div><div class="hcToggleSub">${orientation} · ${innerWidth} × ${innerHeight}px</div></div></div><div class="hcToggleRow"><div><div class="hcToggleTitle">Conectividad</div><div class="hcToggleSub">${navigator.onLine?"En línea":"Sin conexión"}</div></div></div></div><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Navegador</div><div class="hcSettingsCode">${esc(navigator.userAgent)}</div><div class="hcSettingsNotice">Hospitalito no ofrece todavía revocación remota de sesiones desde esta pantalla.</div></div></div></div>`;
+  }
+  function storageStats(){let bytes=0,count=0,rows=[];try{Object.keys(localStorage).forEach(k=>{const v=localStorage.getItem(k)||"";const b=(k.length+v.length)*2;bytes+=b;count++;rows.push([k,b]);});}catch(_){ }rows.sort((a,b)=>b[1]-a[1]);return {bytes,count,rows};}
+  function fmtBytes(n){if(n<1024)return `${n} B`;if(n<1048576)return `${(n/1024).toFixed(1)} KB`;return `${(n/1048576).toFixed(2)} MB`;}
+  function renderData(){const st=storageStats();return `<div class="hcSettingsPage">${hero("Datos y almacenamiento","Estado de la caché local. Los datos clínicos principales continúan en el backend institucional.")}<div class="hcSettingsGrid"><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Uso local aproximado</div><div style="font-size:30px;font-weight:900;margin:8px 0;">${esc(fmtBytes(st.bytes))}</div><div class="hcSettingsMuted">${st.count} claves en localStorage</div><div class="hcSettingsNotice">Hospitalito usa caché clínica acotada para evitar saturar el navegador.</div></div><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Preferencias visuales</div><div class="hcSettingsCardSub">Esta acción no toca pacientes, prescripciones ni backend.</div><div class="hcSettingsActions"><button class="hcSettingsBtn" id="hcsResetVisualPrefs">Restablecer preferencias visuales</button></div></div></div></div>`;}

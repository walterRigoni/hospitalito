+    if(!memberChat)return;const other=findUserByUsername?.(memberChat);if(!other)return;const text=String(document.getElementById("hcsChatText")?.value||"").trim();const file=document.getElementById("hcsChatImg")?.files?.[0]||null;if(!text&&!file)return;const chats=getChats?.()||{};const k=typeof chatKey==="function"?chatKey(meName(),memberChat):[meName(),memberChat].sort().join("__");const arr=Array.isArray(chats[k])?chats[k]:[];if(file){const dataURL=await readFileAsDataURL(file);arr.push({id:typeof uid==="function"?uid("msg"):String(Date.now()),ts:typeof isoNowLocalFull==="function"?isoNowLocalFull():new Date().toISOString(),from:meName(),kind:"image",text,dataURL});}else{arr.push({id:typeof uid==="function"?uid("msg"):String(Date.now()),ts:typeof isoNowLocalFull==="function"?isoNowLocalFull():new Date().toISOString(),from:meName(),kind:"text",text});}while(arr.length>300)arr.shift();chats[k]=arr;setChats?.(chats);renderSection("members");
+  }
+  function setPref(name,value){const p=loadPrefs();p[name]=value;savePrefs(p);applyPrefs();}
+  function resetVisualPrefs(){try{localStorage.removeItem(prefKey());localStorage.removeItem(themeKey());}catch(_){ }const users=getUsers?.()||[];const idx=users.findIndex(u=>String(u.username||"")===meName());if(idx>=0){const old=users[idx];users[idx]={...old,uiTheme:"blue-dark",preferences:{...(old.preferences||{}),theme:"blue-dark"}};setUsers?.(users);}applyTheme("blue-dark");applyPrefs();renderSection("data");}
+
+  document.addEventListener("click",async function(ev){
+    const open=ev.target.closest?.("#btnOpenSettings");if(open){ev.preventDefault();openSettings("account");return;}
+    if(ev.target.closest?.("#hcSettingsCloseBtn")){closeSettings();return;}
+    if(ev.target.closest?.("#hcSettingsMenuBtn")){document.getElementById("hcSettingsModal")?.classList.toggle("nav-open");return;}
+    if(ev.target.id==="hcSettingsNavBackdrop"){document.getElementById("hcSettingsModal")?.classList.remove("nav-open");return;}
+    const sec=ev.target.closest?.("[data-hcs-section]");if(sec){setSection(sec.getAttribute("data-hcs-section")||"account");return;}
+    if(ev.target.closest?.("#hcSettingsBackToAccount")){closeSettings();openAccount?.();return;}
+    if(ev.target.closest?.("#hcsOpenClassicAccount")){closeSettings();openAccount?.();return;}
+    const themeBtn=ev.target.closest?.("[data-hcs-theme]");if(themeBtn){await persistTheme(themeBtn.getAttribute("data-hcs-theme"));return;}
+    if(ev.target.closest?.("#hcsSaveProfile")){await saveProfile();return;}
+    if(ev.target.closest?.("#hcsChangePassword")){await changePasswordNew();return;}
+    const mem=ev.target.closest?.("[data-hcs-member]");if(mem){selectedMember=mem.getAttribute("data-hcs-member")||"";memberChat="";renderSection("members");return;}
+    if(ev.target.closest?.("#hcsMembersBack")){selectedMember="";memberChat="";renderSection("members");return;}
+    const chat=ev.target.closest?.("[data-hcs-chat]");if(chat){memberChat=chat.getAttribute("data-hcs-chat")||"";selectedMember=memberChat;renderSection("members");return;}
+    if(ev.target.closest?.("#hcsChatBack")){memberChat="";renderSection("members");return;}
+    if(ev.target.closest?.("#hcsChatSend")){await sendMemberChat();return;}
+    const sw=ev.target.closest?.("[data-hcs-switch-inst]");if(sw){const id=sw.getAttribute("data-hcs-switch-inst")||"";const rows=typeof hcGlobalStoredInstitutions==="function"?hcGlobalStoredInstitutions():[];closeSettings();if(typeof hcInstitutionChooserOpen==="function")hcInstitutionChooserOpen(rows,{switching:true});return;}
+    if(ev.target.closest?.("#hcsSaveSignature")){await saveSignatureNew(false);return;}
+    if(ev.target.closest?.("#hcsRemoveSignature")){if(confirm("¿Quitar la firma guardada de su cuenta?"))await saveSignatureNew(true);return;}
+    if(ev.target.closest?.("#hcsResetVisualPrefs")){if(confirm("¿Restablecer solamente las preferencias visuales de este usuario?"))resetVisualPrefs();return;}
+    if(ev.target.closest?.("#hcsCopyDiagnostics")){const txt=diagnosticsText();try{await navigator.clipboard.writeText(txt);toast?.("Diagnóstico copiado");}catch(_){prompt("Copiar diagnóstico:",txt);}return;}
+    if(ev.target.closest?.("#hcsLogout")){closeSettings();logout?.();return;}
+  },true);
+  document.addEventListener("change",async function(ev){
+    if(ev.target.id==="hcsProfilePhoto"){const f=ev.target.files?.[0]||null;pendingProfilePhoto=f||null;return;}
+    if(ev.target.id==="hcsSignatureFile"){
+      const f=ev.target.files?.[0]||null;if(!f){pendingSignature=null;return;}
+      try{pendingSignature=typeof hcPrepareSignatureDataURL==="function"?await hcPrepareSignatureDataURL(f):await readFileAsDataURL(f);msg("hcsSignatureMsg","Firma preparada. Presioná Guardar firma y datos.","good");}catch(e){pendingSignature=null;msg("hcsSignatureMsg",e.message||String(e),"warn");}
+      return;
+    }
+    if(ev.target.id==="hcsShowSync"){setPref("showSyncStatus",!!ev.target.checked);return;}
+    if(ev.target.id==="hcsHighContrast"){setPref("highContrast",!!ev.target.checked);return;}
+    if(ev.target.id==="hcsLargeText"){setPref("largeText",!!ev.target.checked);return;}
+    if(ev.target.id==="hcsReduceMotion"){setPref("reduceMotion",!!ev.target.checked);return;}
+  },true);
+  document.addEventListener("input",function(ev){if(ev.target.id==="hcsMemberSearch"){const q=String(ev.target.value||"").trim().toLowerCase();document.querySelectorAll("#hcsMemberList [data-search]").forEach(el=>{el.style.display=!q||String(el.dataset.search||"").includes(q)?"":"none";});}},true);
+  document.addEventListener("keydown",function(ev){if(ev.key==="Escape"&&document.getElementById("hcSettingsModal")?.classList.contains("show")){const modal=document.getElementById("hcSettingsModal");if(modal.classList.contains("nav-open"))modal.classList.remove("nav-open");else closeSettings();}},true);
+  window.addEventListener("resize",()=>{if(currentSection==="sessions"&&document.getElementById("hcSettingsModal")?.classList.contains("show"))renderSection("sessions");},{passive:true});
+
+  window.hcSettingsOpen=openSettings;window.hcSettingsClose=closeSettings;window.hcSettingsApplyTheme=applyTheme;
+  initialTheme();
+  setTimeout(initialTheme,100);setTimeout(initialTheme,700);
+  console.info("HC v228: Configuración a pantalla completa, 7 temas y auditoría segura activas.");
+})();
+</script>
+
 </body>
 </html>

+  function diagnosticsText(){const st=storageStats();return [`Hospitalito ${window.HC_DEMO_VERSION||"v228"}`,`Usuario: ${session?.username||"—"} (${session?.role||"—"})`,`Institución: ${currentInstitutionName()}`,`Backend: ${document.getElementById("backendStatus")?.textContent?.trim()||"—"}`,`Online: ${navigator.onLine?"sí":"no"}`,`Viewport: ${innerWidth}x${innerHeight}`,`Tema: ${userTheme(currentUser())}`,`localStorage: ${fmtBytes(st.bytes)} / ${st.count} claves`,`Paciente seleccionado: ${typeof selectedPatientId!=="undefined"&&selectedPatientId?selectedPatientId:"ninguno"}`].join("\n");}
+  function renderDiagnostics(){return `<div class="hcSettingsPage">${hero("Diagnóstico y acerca de","Resumen técnico no sensible para detectar problemas de interfaz o sincronización.")}<div class="hcSettingsGrid"><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Hospitalito</div><div class="hcSettingsCode" id="hcsDiagnosticsText">${esc(diagnosticsText())}</div><div class="hcSettingsActions"><button class="hcSettingsBtn" id="hcsCopyDiagnostics">Copiar diagnóstico</button></div></div><div class="hcSettingsCard"><div class="hcSettingsCardTitle">Revisión v228</div><div class="hcSettingsNotice good">✓ JavaScript validado sintácticamente.</div><div class="hcSettingsNotice good">✓ IDs duplicados del modal de Laboratorio corregidos.</div><div class="hcSettingsNotice">La revisión automática evita tocar algoritmos clínicos o sincronización cuando no hay un bug concreto verificable.</div></div></div></div>`;}
+  function renderLogout(){return `<div class="hcSettingsPage">${hero("Cerrar sesión","Finaliza la sesión actual en este navegador.")}<div class="hcSettingsCard" style="max-width:620px;"><div class="hcSettingsCardTitle">Salir de Hospitalito</div><div class="hcSettingsCardSub">No elimina historias clínicas ni desconfigura la institución del equipo.</div><div class="hcSettingsActions"><button class="hcSettingsBtn danger" id="hcsLogout" type="button">Cerrar sesión</button></div></div></div>`;}
+  function renderSection(id){
+    const box=document.getElementById("hcSettingsContent");if(!box)return;
+    const map={account:renderAccount,privacy:renderPrivacy,members:renderMembers,institutions:renderInstitutions,signature:renderSignature,personalization:renderPersonalization,notifications:renderNotifications,accessibility:renderAccessibility,sessions:renderSessions,data:renderData,diagnostics:renderDiagnostics,logout:renderLogout};
+    box.innerHTML=(map[id]||renderAccount)();
+    if(id==="members"&&memberChat){setTimeout(()=>{const m=document.getElementById("hcsChatMsgs");if(m)m.scrollTop=m.scrollHeight;},0);}
+  }
+  function msg(id,text,kind=""){const el=document.getElementById(id);if(!el)return;el.textContent=text;el.className="hcSettingsNotice"+(kind?` ${kind}`:"");}
+  async function saveProfile(){
+    if(!session)return;const u=currentUser();let photo=u.photo||"";
+    try{const f=document.getElementById("hcsProfilePhoto")?.files?.[0];if(f){if(f.size>8*1024*1024)throw new Error("La foto supera 8 MB.");photo=await readFileAsDataURL(f);}}
+    catch(e){return msg("hcsProfileMsg",e.message||String(e),"warn");}
+    const patch={name:String(document.getElementById("hcsProfileName")?.value||u.name||"").trim(),photo,professionalTitle:String(document.getElementById("hcsProfessionalTitle")?.value||"").trim(),licenseNumber:String(document.getElementById("hcsLicenseNumber")?.value||"").trim(),specialty:String(document.getElementById("hcsSpecialty")?.value||"").trim(),uiTheme:userTheme(u),preferences:{...(u.preferences||{}),theme:userTheme(u)}};
+    try{
+      let updated={...u,...patch};
+      const users=getUsers?.()||[];const idx=users.findIndex(x=>String(x.username||"")===meName());if(idx>=0){updated=typeof hcUserUpdateStamp==="function"?hcUserUpdateStamp(users[idx],patch):updated;users[idx]=updated;setUsers?.(users);}
+      if(!session.localSeed&&typeof hcGlobalAuthToken==="function"&&hcGlobalAuthToken()){
+        const r=await hcGlobalAccountRequest("updateProfile",{token:hcGlobalAuthToken(),user:patch});updated={...updated,...(r.user||{})};try{hcGlobalCacheUser?.(updated,{id:hcGlobalCurrentInstitutionId?.(),name:session?.institutionName||""});saveJSON?.(LS.globalAuthUser,updated);}catch(_){ }
+      }
+      session.name=updated.name||session.name;saveJSON?.(LS.session,session);syncSessionUI?.();msg("hcsProfileMsg","Perfil guardado correctamente.","good");
+    }catch(e){msg("hcsProfileMsg",e.message||String(e),"warn");}
+  }
+  async function changePasswordNew(){
+    const cur=String(document.getElementById("hcsPassCurrent")?.value||""),next=String(document.getElementById("hcsPassNew")?.value||""),next2=String(document.getElementById("hcsPassNew2")?.value||"");
+    if(!cur||!next||!next2)return msg("hcsPassMsg","Completá los tres campos.","warn");if(next!==next2)return msg("hcsPassMsg","Las nuevas contraseñas no coinciden.","warn");
+    if(session?.localSeed)return msg("hcsPassMsg","El admin semilla es una cuenta técnica local. Su contraseña no se modifica desde aquí.","warn");
+    try{await hcGlobalAccountRequest("changePassword",{token:hcGlobalAuthToken(),currentPassword:cur,newPassword:next});["hcsPassCurrent","hcsPassNew","hcsPassNew2"].forEach(id=>{const e=document.getElementById(id);if(e)e.value="";});msg("hcsPassMsg","Contraseña global actualizada.","good");}catch(e){msg("hcsPassMsg",e.message||String(e),"warn");}
+  }
+  async function saveSignatureNew(remove=false){
+    const u=currentUser();let signature=remove?"":(pendingSignature||u.signature||"");
+    const patch={signature,professionalTitle:String(document.getElementById("hcsSigTitle")?.value||u.professionalTitle||"").trim(),licenseNumber:String(document.getElementById("hcsSigLicense")?.value||u.licenseNumber||"").trim(),specialty:String(document.getElementById("hcsSigSpecialty")?.value||u.specialty||"").trim(),uiTheme:userTheme(u),preferences:{...(u.preferences||{}),theme:userTheme(u)}};
+    try{let updated={...u,...patch};const users=getUsers?.()||[];const idx=users.findIndex(x=>String(x.username||"")===meName());if(idx>=0){updated=typeof hcUserUpdateStamp==="function"?hcUserUpdateStamp(users[idx],patch):updated;users[idx]=updated;setUsers?.(users);}if(!session?.localSeed&&typeof hcGlobalAuthToken==="function"&&hcGlobalAuthToken()){const r=await hcGlobalAccountRequest("updateProfile",{token:hcGlobalAuthToken(),user:patch});updated={...updated,...(r.user||{})};try{hcGlobalCacheUser?.(updated,{id:hcGlobalCurrentInstitutionId?.(),name:session?.institutionName||""});saveJSON?.(LS.globalAuthUser,updated);}catch(_){ }}pendingSignature=null;msg("hcsSignatureMsg",remove?"Firma eliminada.":"Firma y datos profesionales guardados.","good");renderSection("signature");}catch(e){msg("hcsSignatureMsg",e.message||String(e),"warn");}
+  }
+  async function sendMemberChat(){
